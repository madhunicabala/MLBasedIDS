# MLBasedIDS
Training a model offline (already preprocessed) to detect DOS/DDOS attacks ONLY and  integrate into a “live” IDS pipeline that consumes new flow rows and raises alerts. Study false positives/negatives — from a security operations perspective. 
